package com.bookstore.client;

/**
 * Główna klasa aplikacji klienta.
 * <p>
 * Odpowiada za uruchomienie aplikacji klienta.
 */
public class Main extends ClientApplication {
    /**
     * Główna metoda uruchamiająca aplikację klienta.
     *
     * @param args Argumenty przekazywane do aplikacji (nie są używane).
     */
    public static void main(String[] args) {
        launch();
    }
}
